---
title: Setting Up
description: Get Python installed and ready to code
nextExercise: hello-world
---

# Setting Up Python

Welcome to your first Python lesson! Before we can start coding, we need to make sure Python is installed on your computer.

## What is Python?

Python is a high-level, interpreted programming language that emphasizes code readability and simplicity. It's one of the most popular languages in the world, used for:

- Web development
- Data science and machine learning
- Automation and scripting
- Cybersecurity tools
- And much more!

## Installing Python

### Windows

1. Visit [python.org](https://python.org)
2. Download the latest Python 3.x installer
3. Run the installer
4. ⚠️ **Important**: Check "Add Python to PATH" during installation

### macOS

Python comes pre-installed on macOS, but you'll want the latest version:

```bash
# Using Homebrew
brew install python3
```

### Linux

Most Linux distributions come with Python pre-installed. To check:

```bash
python3 --version
```

If not installed:

```bash
# Ubuntu/Debian
sudo apt update
sudo apt install python3

# Fedora
sudo dnf install python3
```

## Verifying Your Installation

Open your terminal (Command Prompt on Windows) and run:

```bash
python3 --version
```

You should see output like:

```
Python 3.11.5
```

## Your First Command

Try running Python interactively:

```bash
python3
```

This opens the Python REPL (Read-Eval-Print Loop). Try typing:

```python
print("Hello, World!")
```

Press Enter, and you should see:

```
Hello, World!
```

To exit the REPL, type `exit()` or press `Ctrl+D` (Linux/Mac) or `Ctrl+Z` (Windows).

## What's Next?

Now that Python is installed, you're ready to write your first program! In the next exercise, we'll create your first Python script.

---

**Challenge**: Try printing your name using the `print()` function in the Python REPL!

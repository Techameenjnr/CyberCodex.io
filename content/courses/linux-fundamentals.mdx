---
title: "Linux Fundamentals"
description: "Learn Linux system administration and command line from scratch to advanced techniques"
category: "linux"
difficulty: "beginner"
duration: "5 hours"
author: "CyberCodex Team"
publishedAt: "2024-11-04"
tags: ["linux", "command line", "bash", "system administration", "terminal", "ubuntu"]
prerequisites: ["Basic computer skills"]
learningObjectives:
  - "Navigate the Linux file system with confidence"
  - "Master essential command line tools"
  - "Manage users, permissions, and processes"
  - "Write basic bash scripts"
  - "Understand Linux security fundamentals"
---

## Introduction

Linux powers the majority of servers, cloud infrastructure, and cybersecurity tools. Whether you're aspiring to be a developer, system administrator, or cybersecurity professional, Linux skills are essential.

This course will transform you from a Linux beginner to a confident command line user.

## Why Learn Linux?

### Industry Standard

- 90% of cloud infrastructure runs on Linux
- Essential for cybersecurity and penetration testing
- Powers Android, supercomputers, and embedded systems

### Career Opportunities

- DevOps Engineers
- System Administrators
- Cybersecurity Professionals
- Cloud Engineers
- Software Developers

## Getting Started

### Linux Distributions

Popular distributions for learning:

- **Ubuntu** - Beginner-friendly, great community
- **Kali Linux** - Security testing and pentesting
- **CentOS/Rocky** - Enterprise server environments
- **Debian** - Stable and reliable

### Installation Options

- Dual boot with Windows
- Virtual machine (VirtualBox, VMware)
- WSL (Windows Subsystem for Linux)
- Cloud instance (AWS, DigitalOcean)

## The Linux File System

### Directory Structure

```
/          Root directory
/home      User home directories
/root      Root user home
/etc       Configuration files
/var       Variable data (logs)
/tmp       Temporary files
/usr       User programs
/bin       Essential binaries
```

### Navigation Commands

**Change directory:**
```bash
cd /home/user
cd ..           # Go up one level
cd ~            # Go to home directory
```

**List files:**
```bash
ls              # List files
ls -la          # Detailed list with hidden files
ls -lh          # Human-readable file sizes
```

**Print working directory:**
```bash
pwd
```

## Essential Commands

### File Operations

**Create files:**
```bash
touch file.txt
echo "Hello" > file.txt
```

**Copy files:**
```bash
cp source.txt destination.txt
cp -r folder1 folder2    # Copy directories
```

**Move/rename:**
```bash
mv oldname.txt newname.txt
mv file.txt /home/user/
```

**Delete:**
```bash
rm file.txt
rm -r folder/           # Delete directory
rm -rf folder/          # Force delete (use carefully!)
```

### Viewing Files

```bash
cat file.txt            # Display entire file
less file.txt           # View file page by page
head file.txt           # First 10 lines
tail file.txt           # Last 10 lines
tail -f /var/log/syslog # Follow log file
```

### Searching

**Find files:**
```bash
find /home -name "*.txt"
locate filename
```

**Search content:**
```bash
grep "search term" file.txt
grep -r "pattern" /path/    # Recursive search
```

## Users and Permissions

### User Management

**Add user:**
```bash
sudo useradd username
sudo passwd username
```

**Switch user:**
```bash
su - username
sudo -i              # Become root
```

### File Permissions

Understanding `rwx`:
- **r** = read (4)
- **w** = write (2)
- **x** = execute (1)

**Change permissions:**
```bash
chmod 755 script.sh     # rwxr-xr-x
chmod +x script.sh      # Make executable
```

**Change ownership:**
```bash
chown user:group file.txt
```

## Process Management

### Viewing Processes

```bash
ps aux                  # All processes
top                     # Real-time monitoring
htop                    # Interactive process viewer
```

### Managing Processes

```bash
kill 1234               # Kill process by PID
killall firefox         # Kill by name
bg                      # Background process
fg                      # Foreground process
```

## Package Management

### Debian/Ubuntu (apt)

```bash
sudo apt update                 # Update package list
sudo apt upgrade               # Upgrade packages
sudo apt install package       # Install package
sudo apt remove package        # Remove package
```

### Red Hat/CentOS (yum/dnf)

```bash
sudo yum update
sudo yum install package
```

## Basic Bash Scripting

### Your First Script

```bash
#!/bin/bash

echo "Hello, World!"
echo "Current directory: $(pwd)"
echo "Current user: $(whoami)"
```

**Make it executable:**
```bash
chmod +x script.sh
./script.sh
```

### Variables and Conditionals

```bash
#!/bin/bash

NAME="Linux"
echo "Learning $NAME"

if [ -f "file.txt" ]; then
    echo "File exists"
else
    echo "File not found"
fi
```

## Networking Commands

```bash
ip addr                 # Show IP addresses
ping google.com         # Test connectivity
netstat -tulpn         # Show network connections
ssh user@host          # Remote login
scp file.txt user@host:/path  # Secure copy
```

## Text Editors

### Nano (Beginner-friendly)
```bash
nano file.txt
# Ctrl+O to save, Ctrl+X to exit
```

### Vim (Advanced)
```bash
vim file.txt
# Press 'i' for insert mode
# Press 'Esc' then ':wq' to save and exit
```

## Security Basics

### Firewall

```bash
sudo ufw enable
sudo ufw allow 22/tcp       # Allow SSH
sudo ufw status
```

### System Updates

Keep your system secure:
```bash
sudo apt update && sudo apt upgrade -y
```

## Practical Projects

Throughout this course, you'll build:

- A personal file organization system
- Automated backup scripts
- System monitoring tools
- Custom command aliases
- Server administration skills

## Next Steps

Continue your Linux journey:

- Advanced bash scripting
- Server administration
- Docker and containers
- Linux security hardening
- Kubernetes and orchestration

Master the command line, and you'll unlock unlimited potential in tech!
